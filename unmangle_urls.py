import sys; import re; from urllib.parse import urlparse, parse_qs, unquote; list(print(re.sub('_', '/', unquote(re.sub('-', '%', list(parse_qs(q.query)['u'][0] if ('u' in parse_qs(q.query)) else int('5', 2) for q in (urlparse(mangled_url.strip()),))[0])))) for mangled_url in sys.stdin.readlines())
